apiVersion: v1
kind: Service
metadata:
  name: {{ include "kdi-webapp.fullname" . }}
  labels:
    {{- include "kdi-webapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      nodePort: 30080
  selector:
    {{- include "kdi-webapp.selectorLabels" . | nindent 4 }}
